import React from 'react';
import ContentLayout from '../Layouts/Content';

export const AboutPage = () => {
  return (
    <div>{'About content'}</div>
  );
};

export default ContentLayout(AboutPage);
